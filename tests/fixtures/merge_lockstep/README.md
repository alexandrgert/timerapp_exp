# merge_lockstep fixtures

Golden JSON pairs for desktop ↔ Android richer-session merge lockstep.

Keep an identical tree under `android/app/src/test/resources/merge_lockstep/`.
When editing a case here, copy the same files into the Android resources path.
